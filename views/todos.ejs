<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Todo List - Todo App</title>
  <link rel="stylesheet" href="/styles.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>
        <span class="material-icons" style="font-size: 3rem; vertical-align: middle; margin-right: 16px;">checklist</span>
        My Todo List
      </h1>
      <p>Manage your tasks efficiently</p>
    </div>

    <div class="card">
      <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px;">
        <h2 style="color: #1976d2; margin: 0;">Your Tasks</h2>
        <a href="/todos/new" class="btn btn-primary">
          <span class="material-icons" style="vertical-align: middle; margin-right: 8px;">add</span>
          Add New Task
        </a>
      </div>

      <% if (todos && todos.length > 0) { %>
        <div style="margin-bottom: 16px;">
          <p style="color: #666; font-size: 0.9rem;">
            <span class="material-icons" style="vertical-align: middle; margin-right: 4px; font-size: 1rem;">info</span>
            You have <%= todos.length %> task<%= todos.length === 1 ? '' : 's' %>
          </p>
        </div>
        
        <div>
          <% todos.forEach(function(todo) { %>
            <div class="todo-item">
              <div class="todo-title">
                <h3 style="margin: 0; color: #333; font-weight: 500;">
                  <span class="material-icons" style="vertical-align: middle; margin-right: 8px; color: #1976d2;">task</span>
                  <%= todo.title %>
                </h3>
                <p style="margin: 4px 0 0 0; color: #666; font-size: 0.9rem;">
                  <span class="material-icons" style="vertical-align: middle; margin-right: 4px; font-size: 0.8rem;">schedule</span>
                  Created: <%= new Date(todo.created_at).toLocaleDateString() %>
                </p>
              </div>
              <div class="todo-actions">
                <a href="/todos/<%= todo.id %>/edit" class="btn btn-secondary" style="padding: 8px 16px;">
                  <span class="material-icons" style="vertical-align: middle; font-size: 1.2rem;">edit</span>
                </a>
                <form action="/todos/<%= todo.id %>/delete" method="POST" style="display: inline;">
                  <button type="submit" class="btn btn-danger" style="padding: 8px 16px;" 
                          onclick="return confirm('Are you sure you want to delete this task?')">
                    <span class="material-icons" style="vertical-align: middle; font-size: 1.2rem;">delete</span>
                  </button>
                </form>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <div style="text-align: center; padding: 40px 20px;">
          <span class="material-icons" style="font-size: 4rem; color: #ddd; margin-bottom: 16px;">assignment</span>
          <h3 style="color: #666; margin-bottom: 8px;">No tasks yet</h3>
          <p style="color: #999; margin-bottom: 24px;">Start by adding your first task!</p>
          <a href="/todos/new" class="btn btn-primary">
            <span class="material-icons" style="vertical-align: middle; margin-right: 8px;">add</span>
            Add Your First Task
          </a>
        </div>
      <% } %>
    </div>

    <div class="nav-links">
      <a href="/">
        <span class="material-icons" style="vertical-align: middle; margin-right: 4px; font-size: 1rem;">home</span>
        Back to Home
      </a>
    </div>
  </div>
</body>
</html> 